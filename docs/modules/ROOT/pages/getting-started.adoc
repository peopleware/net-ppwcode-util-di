= Getting started

== Add NuGet package

This package is available on NuGet: https://www.nuget.org/packages/PPWCode.Util.DI.I[PPWCode.Util.DI.I].

You can use your IDE to add it as a dependency to your project, or manually add it in the `.csproj` file.  This would look similar to the following:

[source,xml]
----
include::example$project.csproj[]
----

After the package is added, you can use the helper classes from `PPWCode.Util.DI.I` in your code.
